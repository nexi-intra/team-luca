import DocPlaceholder from "@/app/(docs)/components/DocPlaceholder";

export default function UserManagementPage() {
  return (
    <DocPlaceholder
      title="User Management"
      description="Create, manage, and organize users and groups within the system"
      ring="admins"
    />
  );
}
