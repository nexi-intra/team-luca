import DocPlaceholder from '@/app/(docs)/components/DocPlaceholder'

export default function ExtensionsPage() {
  return (
    <DocPlaceholder
      title="Extensions"
      description="Build custom extensions and plugins to extend platform functionality"
      ring="developers"
    />
  )
}